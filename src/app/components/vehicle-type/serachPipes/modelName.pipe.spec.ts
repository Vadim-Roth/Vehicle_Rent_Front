import { ModelNameSearch } from './modelName.pipe';

describe('SearchingPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelNameSearch();
    expect(pipe).toBeTruthy();
  });
});
